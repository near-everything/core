{
  "name": "core",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"pnpm run dev:apps\" \"pnpm dev:widgets\"",
    "dev:apps": "npx nx run-many --target=dev --projects=gateway",
    "dev:widgets": "VIEWER_PORT=3005 node tools/near-social-local-viewer/init-viewer.js widgets",
    "build": "npx nx run --target=build --projects=gateway"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1"
  },
  "devDependencies": {
    "nx": "^15.9.2"
  }
}
